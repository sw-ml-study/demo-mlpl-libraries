#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
"$repo_root/scripts/validate-catalog" "$repo_root/catalog/libraries.toml"
"$repo_root/scripts/validate-lock" "$repo_root/tests/fixtures/valid.lock.toml"
echo "PASS library catalog and lock contracts"
