Record that upstream sw-mlpl fixed the S4 empty-array broadcast crash (commit 1ce43dc2) and that every other recorded host gap is still absent in the rebuilt interpreter. Update docs/sw-mlpl-requests.md (S4 resolved with the commit and the retained guard rationale, status line, upstream RS labels where they map to our items), docs/requests-response.md (host-gap paragraph), docs/jsonl.md and docs/checkpoint.md if they describe S4 as open, and docs/demo-extensions-requests.md status if affected. Verify by probe which builtins exist rather than assuming. Do not mutate sibling repositories. Run just check, commit, push, then agentrail complete.