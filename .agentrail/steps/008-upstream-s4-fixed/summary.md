Recorded the upstream S4 fix (sw-mlpl 1ce43dc2) after re-probing every host gap against the rebuilt interpreter at sw-mlpl a5026255: S4 is total for rank-1 and rank-2 empty arrays; S1, S2, S3, S5, S6, S7 builtins are all still absent, so every workaround and the text retirement probe remain valid. Marked S4 resolved with the rationale for keeping the empty-array guards (vendored libraries run on older consumer interpreters), refreshed the status line, mapped upstream RS9 to our S1, and recorded that upstream routes RS7/RS8 here although the consuming repository has not requested them. Updated docs/requests-response.md. Documentation only: library sources untouched so pinned vendored fixtures stay valid. just check passes, 39 tests pass; commit 09246e8 pushed to origin/main.