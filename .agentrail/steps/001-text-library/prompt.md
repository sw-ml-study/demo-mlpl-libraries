Implement the L1 text helper library test-first under docs/library-contract.md. Create lib/text/text.mlpl (prefix u:text_, capabilities ["core.include.v1"], dependencies []), tests/test_text.mlpl with native mlplunit tests, docs/text.md, an examples/ script, and the catalog entry (version 0.1.0). Provide u:text_is_empty, u:text_trim, u:text_trim_left, u:text_trim_right, u:text_starts_with, u:text_ends_with, u:text_contains, bounded u:text_replace_all(text, needle, replacement, max_replacements), u:text_pad_left, u:text_pad_right, u:text_split_lines (LF and CRLF), and character-class tests u:text_is_digit, u:text_is_letter, u:text_is_whitespace, built only on existing builtins (str_len, str_slice, str_find, str_concat, str_split, str_join, chars). Require character-indexed behavior on Unicode input, totality on empty input and empty needles (never a hard error), and a retirement test that pins the absence of native str_replace, str_trim, str_starts_with, and str_contains so the library can retire when core ships them. Treat ../reasoning-from-scratch/probes/str-helpers.mlpl and ../demo-ml-utils/src/training/agentrail_help_drift.mlpl as read-only evidence. Any builtin gap goes to docs/sw-mlpl-requests.md, never upstream. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete.
