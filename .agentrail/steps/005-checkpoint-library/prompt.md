Implement the L4 per-tensor MLPB checkpoint library test-first under docs/library-contract.md. Create lib/checkpoint/ (entry lib/checkpoint/checkpoint.mlpl, prefix u:ckpt_, dependencies []), tests/test_checkpoint.mlpl, docs/checkpoint.md, an examples/ script, and the catalog entry (version 0.1.0). Declare capabilities ["core.include.v1", "fs.read-bounded.v1"] plus one new stable write capability identifier (for example fs.write-atomic.v1) that you define in docs/library-contract.md and accept in scripts/validate-catalog. Provide u:ckpt_save(dir, record, opts) that writes each named array as one to_native file beneath dir with write_atomic, then a JSON index and a manifest of byte sizes and sha256 digests, in a deterministic name order; u:ckpt_load(dir, opts) that reads the index and manifest under budgets, verifies every member's size and digest before parse_native, and returns ok(record) or a structured err naming the member and cause; and u:ckpt_verify(dir, opts) that checks without decoding. Tests must cover round-trip equality for several dtypes and shapes, empty records, missing, extra, and tampered members, a truncated index, budget excess, missing directories, sandbox traversal, and a capability-negative path. ../reasoning-from-scratch/probes/native-roundtrip-10mb.mlpl is read-only evidence for the primitive. Builtin gaps go to docs/sw-mlpl-requests.md. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete.
