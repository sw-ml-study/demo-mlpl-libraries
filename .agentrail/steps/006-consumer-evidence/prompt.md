Produce executable consumer evidence and answer the requesting repository. For each library added in this saga (text, jsonl, safetensors-header, checkpoint), install it into an integration/ consumer fixture with scripts/install-library at a pinned revision, commit the generated swml.lock.toml, and add a consumer test to the gate (mirroring tests/test-native3d-vendored) that runs the vendored code and verifies the lock with --check. Update README.md, catalog/README.md, and docs/plan.md to list the new libraries. Write docs/requests-response.md answering ../reasoning-from-scratch/docs/demo-mlpl-libraries-requests.md item by item (L1 through L4, and why L5 is not published) with library name, version, prefix, entry, capabilities, and the exact revision to pin. Consolidate every host gap found during the saga into docs/sw-mlpl-requests.md and docs/demo-extensions-requests.md; create them as explicit empty-request records if nothing was needed. Do not mutate any sibling repository. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete --done.
