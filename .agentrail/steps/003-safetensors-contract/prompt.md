Freeze the L3 safetensors header reader migration contract without writing library code. Inventory ../demo-ml-utils read-only: probes/capabilities.mlpl, the archived binary-format-foundations saga steps 003 and 004, tests/safetensors-catalog.mlpl and related tests, src/formats/, and fixtures/safetensors/. Write docs/safetensors-header-migration.md recording the exact public API to publish under prefix u:sth_ (bounded header-length decode from the eight-byte little-endian prefix, header budget, file_size check, two budgeted reads, JSON header validation with budgets, deterministic sorted tensor-name discovery, and validated dtype/shape/data_offsets records), the error shapes, the budgets and their defaults, fixture provenance with the demo-ml-utils revision they come from, the fs.read-bounded.v1 capability, and the boundary that tensor decoding, cataloging, and GGUF conversion remain in demo-ml-utils. Write docs/demo-ml-utils-requests.md asking that repository to adopt the published library by pinned revision once it exists. Add tests/test-safetensors-contract, a shell gate like tests/test-native3d-contract that pins the referenced revision, every u:sth_ function name, and the boundaries. Do not mutate ../demo-ml-utils. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete.
