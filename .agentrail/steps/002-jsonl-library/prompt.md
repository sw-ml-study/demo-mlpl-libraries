Implement the L2 JSONL reader library test-first under docs/library-contract.md. Create lib/jsonl/jsonl.mlpl (prefix u:jsonl_, capabilities ["core.include.v1", "fs.read-bounded.v1"], dependencies []), tests/test_jsonl.mlpl, docs/jsonl.md, an examples/ script, small fixtures under tests/fixtures/, and the catalog entry (version 0.1.0). Provide u:jsonl_parse(text, opts) that splits LF and CRLF lines, applies a blank-line policy from opts, parses each line with parse_json under caller-supplied budgets, requires each record to be a JSON object, and returns ok(list of records) or a structured err carrying the one-based line number and cause; u:jsonl_read(path, opts) that checks file_size against a byte budget, reads with bounded read_bytes and decode_bytes, and delegates to parse; and u:jsonl_take_first(path, n, opts) that returns at most n records without parsing later lines. Carry any private line-splitting helper under the u:jsonl_ prefix rather than including lib/text. Tests must cover empty files, trailing newline, CRLF, blank lines, line-numbered parse errors, non-object records, byte-budget and parse-budget excess, missing paths, and a capability-negative path. ../reasoning-from-scratch/probes/json-array-of-objects.mlpl is read-only evidence for why this idiom exists. Builtin gaps go to docs/sw-mlpl-requests.md. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete.
