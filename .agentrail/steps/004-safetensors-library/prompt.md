Implement the L3 safetensors header reader library test-first against the frozen docs/safetensors-header-migration.md. Create lib/safetensors-header/ (entry lib/safetensors-header/header.mlpl, prefix u:sth_, capabilities ["core.include.v1", "fs.read-bounded.v1"], dependencies []), tests/test_safetensors_header.mlpl, docs/safetensors-header.md, an examples/ script, and the catalog entry (version 0.1.0). Bring the tiny fixtures over with documented provenance (copied from ../demo-ml-utils at the pinned revision or regenerated deterministically by a checked-in MLPL generator) under tests/fixtures/safetensors/. Tests must cover a valid header, truncation before and inside the header, header-budget excess, file_size shorter than the declared header, u64 maximum length, the 2^53 precision hazard, malformed JSON, non-object headers, missing dtype or shape or offsets, duplicate or out-of-order names, missing files, sandbox traversal, and a capability-negative path; memory use must not depend on model size. Update tests/test-safetensors-contract if the frozen API changes and explain why in the commit. Builtin gaps go to docs/sw-mlpl-requests.md; native needs go to docs/demo-extensions-requests.md. Run just check, stage named paths with .agentrail/ state, commit, push, then agentrail complete.
